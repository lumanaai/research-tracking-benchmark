from setuptools import setup, find_packages

setup(
    name='lumixai_analytics',
    version='0.1',
    packages=find_packages(),
    install_requires=[
        # List your project's dependencies here.
        # For example:
        # 'numpy>=1.14.5',
        # 'pandas>=0.23.0',
    ],
)