# Virtualenv (symlink to SSD; never commit)
.venv
venv/
env/

# Python
__pycache__/
*.py[cod]
*$py.class
*.so
*.egg
*.egg-info/
.eggs/
dist/
build/
pip-wheel-metadata/
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
.coverage.*
htmlcov/
.tox/
.nox/
*.log

# Editor / OS
.idea/
.vscode/
*.swp
*.swo
*~
.DS_Store
Thumbs.db

# Model weights & checkpoints (keep on SSD / models/ locally)
models/
*.pt
*.pth
*.ckpt
*.onnx
*.weights
*.pkl
*.h5
*.engine

# Media / caches that may land under the repo
*.mp4
*.avi
*.mov
*.wav
*.gif
*.webm
*.jpg
*.jpeg
.cache/
wandb/
runs/
outputs/
tmp/
temp/
.ipynb_checkpoints/

# SSD MOT artifacts (keep on /media/7TBSSD/data/tracking/; never commit
# even if a script is pointed at the repo root)
/experiments/
/detections/
/visualizations/
/mot/
/weights/
*_visualizations/

# Published comparison tables in results/comparisons/ are source of record
# and should stay tracked. Do not ignore that directory.

# Upstream clone noise / fixtures (not needed to run our pipeline)
**/evaldata/
**/TrackEval/tracker_test/
**/trackeval/tracker_test/

# Nested VCS leftovers (clones are vendored as plain directories, not submodules)
**/.git/
**/.git_bak/
.gitmodules

# Local secrets / env
.env
.env.*
!.env.example
credentials.json
*.pem
secrets/
